#!/bin/bash

temp=$(cat /sys/class/hwmon/hwmon2/temp1_input)
temp=$(($temp / 1000))
if [ $temp -le 65 ]; then
	color="#66E1E3"
elif [ $temp -le 80 ]; then
	color="#FFC125"
else
	color="#FF0000"
fi

printf '{"full_text":"%s", "color":"%s"}\n' ${temp}°C $color
