#!/bin/bash

swayidle -w \
	 timeout 270 'swaymsg "output * dpms off"' \
	 resume 'swaymsg "output * dpms on"' \
	 timeout 300 'systemctl suspend' \
	 before-sleep 'swaylock -f -i ~/Dropbox/images/wallpaper/gorillaz.png'
