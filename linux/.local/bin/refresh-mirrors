#!/bin/bash

curl -s "https://archlinux.org/mirrorlist/?country=MX&country=US&protocol=https&use_mirror_status=on" | sed -e 's/^#Server/Server/' -e '/^#/d' | rankmirrors -n 5 - > /tmp/mirrorlist && mv -f /tmp/mirrorlist /etc/pacman.d/mirrorlist
