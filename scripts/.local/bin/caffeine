#!/bin/bash

IDLE=$(pgrep xidlehook)

if [ -z $IDLE ]; then
    i3-msg "exec --no-startup-id xidle"
else
    kill -9 $IDLE
    coffee="☕"
    color="#B8860B"
fi

printf '{"full_text":"%s", "color":"%s"}\n' "$coffee" "$color"
